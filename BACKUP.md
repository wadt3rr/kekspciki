# Резервное копирование и миграция данных 📦

## Важно: Ваши данные сохранятся!

### Как работает база данных:

1. **SQLite хранит все данные в файле** `database/premia.db`
2. **Функция `initDatabase()` безопасна** - использует `CREATE TABLE IF NOT EXISTS`
   - Таблицы создаются только если их еще нет
   - Существующие данные НЕ удаляются
3. **Скрипт `init-db.js` проверяет данные** перед созданием:
   - Проверяет наличие администратора перед созданием
   - Проверяет наличие номинаций перед созданием
   - Не создает дубликаты

## Что нужно сделать при деплое:

### ✅ Вариант 1: Копирование существующей БД (рекомендуется)

Если у вас уже есть заполненная база данных:

1. **Скопируйте файл базы данных на сервер:**
   ```bash
   # С вашего компьютера
   scp database/premia.db user@your-server:/path/to/kekspciki/database/
   
   # Или через FTP/SFTP клиент
   # Загрузите файл database/premia.db в директорию database/ на сервере
   ```

2. **Убедитесь, что файл имеет правильные права:**
   ```bash
   chmod 644 database/premia.db
   chmod 755 database/
   ```

3. **Запустите сервер:**
   ```bash
   npm start
   ```
   
   База данных будет использована как есть, все данные сохранятся!

### ✅ Вариант 2: Создание новой БД (если нужно начать с нуля)

Если хотите начать с чистой базы:

1. **Удалите старую базу (если есть):**
   ```bash
   rm database/premia.db
   ```

2. **Инициализируйте новую:**
   ```bash
   npm run init-db
   ```

3. **Запустите сервер:**
   ```bash
   npm start
   ```

## Резервное копирование

### Перед деплоем обязательно сделайте бэкап!

```bash
# Создать резервную копию базы данных
cp database/premia.db database/premia.db.backup

# Или с датой
cp database/premia.db database/premia.db.backup.$(date +%Y%m%d)
```

### Регулярное резервное копирование

Рекомендуется настроить автоматическое резервное копирование:

```bash
# Пример скрипта для бэкапа (backup-db.sh)
#!/bin/bash
BACKUP_DIR="/path/to/backups"
DB_PATH="database/premia.db"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR
cp $DB_PATH "$BACKUP_DIR/premia_$DATE.db"

# Удалить старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "premia_*.db" -mtime +30 -delete

echo "Backup created: premia_$DATE.db"
```

Добавьте в crontab для ежедневного бэкапа:
```bash
0 2 * * * /path/to/backup-db.sh
```

## Миграция данных

### Если нужно добавить колонку image_url в существующую БД:

```bash
# Проверить, есть ли колонка
sqlite3 database/premia.db "PRAGMA table_info(candidates);"

# Если колонки image_url нет, запустить миграцию
node scripts/add-image-url-column.js
```

## Что сохраняется в базе данных:

- ✅ Все пользователи (включая администратора)
- ✅ Все номинации
- ✅ Все кандидаты (с их video_url и image_url)
- ✅ Все голоса пользователей
- ✅ Все настройки и метаданные

## Что НЕ сохраняется в базе данных:

- ❌ Загруженные файлы (изображения и видео)
  - Хранятся в `uploads/images/` и `uploads/videos/`
  - **Нужно скопировать эти директории отдельно!**

## Полная миграция проекта:

При переносе проекта на новый сервер нужно скопировать:

1. **База данных:**
   ```bash
   database/premia.db
   ```

2. **Загруженные файлы:**
   ```bash
   uploads/images/
   uploads/videos/
   ```

3. **Конфигурация:**
   ```bash
   .env (с правильным JWT_SECRET)
   ```

4. **Код проекта:**
   ```bash
   Все файлы кроме node_modules/
   ```

## Проверка после миграции:

После копирования данных проверьте:

```bash
# 1. Проверить размер базы данных
ls -lh database/premia.db

# 2. Проверить количество записей
sqlite3 database/premia.db "SELECT COUNT(*) FROM users;"
sqlite3 database/premia.db "SELECT COUNT(*) FROM nominations;"
sqlite3 database/premia.db "SELECT COUNT(*) FROM candidates;"
sqlite3 database/premia.db "SELECT COUNT(*) FROM votes;"

# 3. Проверить наличие файлов
ls -lh uploads/images/
ls -lh uploads/videos/
```

## Важные замечания:

⚠️ **Всегда делайте бэкап перед:**
- Обновлением проекта
- Запуском миграций
- Изменением структуры БД

⚠️ **Файл базы данных:**
- Не коммитьте в Git (уже в .gitignore)
- Храните в безопасном месте
- Регулярно делайте резервные копии

⚠️ **Загруженные файлы:**
- Могут занимать много места
- Копируйте их вместе с базой данных
- Убедитесь, что пути в БД совпадают с реальными файлами

