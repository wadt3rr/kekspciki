# Быстрый старт 🚀

## Установка и запуск за 5 минут

### 1. Установите зависимости
```bash
npm install
```

### 2. Создайте файл .env
Скопируйте `.env.example` в `.env`:
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Откройте `.env` и измените `JWT_SECRET` на случайную строку (например, используйте [генератор](https://randomkeygen.com/)).

### 3. Инициализируйте базу данных
```bash
npm run init-db
```

Это создаст:
- ✅ Базу данных SQLite
- ✅ Администратора: `admin` / `admin123`
- ✅ 8 номинаций по умолчанию

### 4. Запустите сервер
```bash
npm start
```

### 5. Откройте в браузере
```
http://localhost:3000
```

## Первые шаги

1. **Войдите как администратор:**
   - Username: `admin`
   - Password: `admin123`
   - ⚠️ **Смените пароль после первого входа!**

2. **Создайте пользователей:**
   - Нажмите "Войти" → "Регистрация"
   - Создайте аккаунты для друзей

3. **Начните голосование:**
   - Войдите в систему
   - Перейдите в раздел "Голосование"
   - Выберите номинацию и кандидата

## Проблемы?

### База данных не создается
Убедитесь, что у вас есть права на запись в папку `database/`.

### Порт занят
Измените `PORT` в файле `.env` на другой (например, 3001).

### Модули не найдены
Убедитесь, что выполнили `npm install`.

## Следующие шаги

- Прочитайте полную документацию в [README.md](README.md)
- Настройте дату события в `public/script.js`
- Измените номинации через API или базу данных
- Настройте цвета в `public/styles.css`

---

**Готово! Наслаждайтесь премией! 🎉**

