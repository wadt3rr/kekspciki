# Премия Друзей 🏆

Веб-сайт для локальной премии среди друзей, созданный по образцу [SLAY Award](https://slay-award.ru/).

## Описание

Полнофункциональное веб-приложение для проведения премии среди друзей с бэкендом на Node.js/Express, базой данных SQLite, системой авторизации и голосования.

## Возможности

### Фронтенд
- ✨ Современный дизайн с градиентами и анимациями
- ⏱️ Таймер обратного отсчета до церемонии награждения
- 🗳️ Система голосования с авторизацией
- 🔐 Регистрация и вход пользователей
- 📱 Адаптивный дизайн для всех устройств
- ❓ Интерактивный FAQ раздел
- 🎨 Красивые анимации и эффекты

### Бэкенд
- 🔒 JWT авторизация
- 💾 SQLite база данных
- 🗳️ RESTful API для голосований
- 👥 Управление пользователями
- 📋 Управление номинациями
- 🛡️ Защита маршрутов и валидация данных

## Структура проекта

```
kekspciki/
├── public/                 # Фронтенд файлы
│   ├── index.html          # Основная HTML структура
│   ├── styles.css          # Стили и дизайн
│   └── script.js           # JavaScript функциональность
├── database/               # База данных
│   └── db.js               # Настройка SQLite
├── middleware/             # Middleware
│   └── auth.js             # JWT аутентификация
├── routes/                  # API маршруты
│   ├── auth.js             # Авторизация
│   ├── votes.js            # Голосования
│   ├── nominations.js     # Номинации
│   └── users.js            # Пользователи
├── scripts/                # Скрипты
│   └── init-db.js          # Инициализация БД
├── server.js               # Express сервер
├── package.json            # Зависимости
├── .env.example            # Пример конфигурации
└── README.md               # Документация
```

## Установка

### Требования

- Node.js (версия 14 или выше)
- npm или yarn

### Шаги установки

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
NODE_ENV=development
```

**⚠️ Важно:** Измените `JWT_SECRET` на случайную строку для безопасности!

4. **Инициализируйте базу данных:**
```bash
npm run init-db
```

Это создаст:
- Таблицы в базе данных
- Администратора по умолчанию (username: `admin`, password: `admin123`)
- Начальные номинации

**⚠️ Важно:** Смените пароль администратора после первого входа!

5. **Запустите сервер:**
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

6. **Откройте в браузере:**
```
http://localhost:3000
```

## API Endpoints

### Авторизация

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получить текущего пользователя (требует авторизации)
- `GET /api/auth/verify` - Проверить токен

### Голосования

- `POST /api/votes` - Создать/обновить голос (требует авторизации)
- `GET /api/votes/my` - Получить свои голоса (требует авторизации)
- `GET /api/votes/results` - Получить результаты голосования (публичный)
- `GET /api/votes/all` - Получить все голоса (только для админов)
- `DELETE /api/votes/:id` - Удалить голос (требует авторизации)

### Номинации

- `GET /api/nominations` - Получить все активные номинации
- `GET /api/nominations/:id` - Получить номинацию по ID
- `POST /api/nominations` - Создать номинацию (только для админов)
- `PUT /api/nominations/:id` - Обновить номинацию (только для админов)
- `DELETE /api/nominations/:id` - Удалить номинацию (только для админов)

### Пользователи

- `GET /api/users` - Получить всех пользователей (только для админов)
- `GET /api/users/:id` - Получить пользователя по ID

## Использование

### Регистрация и вход

1. Нажмите кнопку "Войти" в навигации
2. Выберите вкладку "Регистрация" для создания нового аккаунта
3. Заполните форму и нажмите "Зарегистрироваться"
4. После регистрации вы автоматически войдете в систему

### Голосование

1. Войдите в систему
2. Перейдите в раздел "Голосование"
3. Выберите номинацию из списка
4. Введите имя кандидата
5. Нажмите "Проголосовать"

**Примечание:** Вы можете проголосовать только один раз в каждой номинации, но можете изменить свой голос.

### Администратор

По умолчанию создается администратор:
- **Username:** `admin`
- **Password:** `admin123`

Администратор может:
- Создавать, редактировать и удалять номинации
- Просматривать все голоса
- Управлять пользователями

## Настройка

### Изменение даты события

Откройте `public/script.js` и измените дату события:

```javascript
const eventDate = new Date('2025-12-31T20:00:00').getTime();
```

### Изменение номинаций

Номинации можно управлять через API или напрямую в базе данных. По умолчанию создаются 8 номинаций при инициализации.

### Изменение цветовой схемы

В файле `public/styles.css` найдите секцию `:root` и измените цвета:

```css
:root {
    --primary-color: #ff006e;
    --secondary-color: #8338ec;
    --accent-color: #3a86ff;
    /* Измените на свои цвета */
}
```

### Создание администратора

Чтобы сделать пользователя администратором, выполните SQL запрос:

```sql
UPDATE users SET is_admin = 1 WHERE username = 'имя_пользователя';
```

Или используйте SQLite CLI:
```bash
sqlite3 database/premia.db
UPDATE users SET is_admin = 1 WHERE username = 'имя_пользователя';
```

## Безопасность

- ✅ Пароли хешируются с помощью bcrypt
- ✅ JWT токены для авторизации
- ✅ Валидация входных данных
- ✅ Защита маршрутов middleware
- ✅ SQL injection защита через параметризованные запросы

**Рекомендации для продакшена:**
- Используйте HTTPS
- Измените JWT_SECRET на случайную строку
- Настройте CORS для вашего домена
- Регулярно делайте резервные копии базы данных
- Используйте переменные окружения для секретов

## Разработка

### Структура базы данных

**users:**
- id (INTEGER PRIMARY KEY)
- username (TEXT UNIQUE)
- email (TEXT UNIQUE)
- password_hash (TEXT)
- display_name (TEXT)
- is_admin (INTEGER)
- created_at (DATETIME)

**nominations:**
- id (INTEGER PRIMARY KEY)
- name (TEXT)
- description (TEXT)
- is_active (INTEGER)
- created_at (DATETIME)

**votes:**
- id (INTEGER PRIMARY KEY)
- user_id (INTEGER, FOREIGN KEY)
- nomination_id (INTEGER, FOREIGN KEY)
- candidate_name (TEXT)
- created_at (DATETIME)
- UNIQUE(user_id, nomination_id)

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## Лицензия

Свободное использование для личных проектов.

## Автор

Создано для локальной премии среди друзей.

---

**Приятного использования! 🎉**
